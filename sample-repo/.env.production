# Fake credentials for the workshop. Not a real key — it is shaped like
# one on purpose, so the guard exercise reads as it would in real life.
NOTIFY_API_URL=https://api.notify-svc.internal/v1
NOTIFY_API_KEY=sk_live_2b71e4c9d8a05f3612cc
RETRY_LIMIT=3